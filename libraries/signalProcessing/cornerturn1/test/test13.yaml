integration_start: 11
sim_frames : 2
# Select the ripple filter to use, corresponds to the register setting
ripple : 0

polynomials:
  # each entry is for a particular virtual channel
  0:
    virtual_channel : 0
    # RFI threshold of 0 means any SPS RFI gets marked at the filterbank output
    # RFI threshold of 2^32-1 = 4294967295 means nothing is marked
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
     
    poly0 : [2643, -0.737, 0, 0, 0, 0]
    # sky frequency in GHz
    sky_freq0 : 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0 : 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0 : 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0 : 10
    valid0 : 1
    #
    poly1: [2643, -0.737, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  1:
    virtual_channel : 1
    RFI_threshold : 4294967295
    poly0: [ 2531, -0.64, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 2531, -0.64, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1

  2:
    virtual_channel : 2
    RFI_threshold : 4294967295
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ -50, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ -50, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  3:
    virtual_channel : 3
    RFI_threshold : 0
    poly0: [ 71, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 71, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
  
  4:
    virtual_channel : 4
    RFI_threshold : 4294967295
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ -1, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ -1, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  5:
    virtual_channel : 5
    RFI_threshold : 4294967295
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 2059, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 2059, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  6:
    virtual_channel : 6
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 2009, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 2009, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  7:
    virtual_channel : 7
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 1984, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 1984, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  8:
    virtual_channel : 8
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 1960, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 1960, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  9:
    virtual_channel : 9
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 1982, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 1982, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
  10:
    virtual_channel : 10
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 2007, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 2007, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1    

  11:
    virtual_channel : 11
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 903, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 903, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1

  12:
    virtual_channel : 12
    RFI_threshold : 0
    # polynomial coefficients c0 to c5, result = c0 + c1*t + ... + c5*t^5
    # Units of ns/s^k
    poly0: [ 4021, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq0: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset0: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset0: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration0: 10
    valid0: 1
    #
    poly1: [ 4021, 0, 0, 0, 0, 0 ]
    # sky frequency in GHz
    sky_freq1: 0.20
    #seconds from the polynomial epoch to the start of the integration period, as a double precision value 
    buf_offset1: 10
    # double precision offset in ns for the second polarisation (relative to the first polarisation). 
    Ypol_offset1: 0
    # Validity time : 32 bit buf_integration: Integration period at which the polynomial becomes valid.
    integration1: 20
    valid1: 1
    
